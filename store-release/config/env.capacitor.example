# Copy to web-v2/.env.capacitor before npm run cap:sync / native builds.
# Baked into the web bundle at build time (not read at runtime on device).

VITE_API_URL=https://cafyz.onrender.com
VITE_APP_URL=https://cafyz.ametronyx.com

# Set true only after adding cap-ios/App/GoogleService-Info.plist
# VITE_NATIVE_PUSH_ENABLED=true
