/** Phone & small tablet — sidebar drawer + hamburger at or below this width (matches --bp-lg). */
export const MOBILE_NAV_MAX_PX = 1024;

/** CSS media query — keep in sync with Sidebar.css / TopBar.css / responsive.css */
export const MOBILE_NAV_MQ = `(max-width: ${MOBILE_NAV_MAX_PX}px)`;
