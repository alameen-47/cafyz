Continue and expand the existing design using the exact same theme, color tokens, typography, spacing, radii, shadows, and component styles already established in this file — do not introduce a new style; everything below must match the look already generated. This is Cafyz, a multi-tenant restaurant management & POS SaaS. Design all of the following screens and components in both dark and light variants, driven by a single shared set of design tokens, fully responsive across desktop, tablet (touch POS, ≥48px tap targets), and mobile, and provide every screen's loading, empty, error, hover, pressed/active, focus, and disabled states. The product has six user roles whose navigation differs — owner, manager, cashier, waiter, kitchen, and founder (super-admin) — and three subscription tiers — Basic, Pro, Premium — that gate which screens appear; reflect this in the sidebar navigation variants and "locked/upgrade" states.

Public / auth screens (standalone, no app chrome): a Login screen (/login) with a desktop split layout (brand hero with logo, stats, and tagline beside a form card) and a single-column mobile layout, supporting three sign-in methods — email + password, a 4-digit PIN with an on-screen numpad, and phone OTP — plus forgot-password and reset-password states and a "New to Cafyz? Trial Request / Purchase Plan" call-to-action; a Register screen (/register); a Get Account / Sign-up screen (/get-account) with plan-picker cards (Basic/Pro/Premium showing feature lists and a "Most Popular" highlight), a 7-day free-trial banner, and an onboarding form for restaurant + owner details; and a public customer QR menu (/m/:restaurantId) that is customer-facing with no login — an elegant hero (restaurant logo, name, tagline, location), a sticky category navigation bar, and menu item cards showing image, name, description, price, and a "★ Popular" badge.

Logged-in app shell: a persistent left sidebar (logo, role-based nav items, plan badge, user block, theme toggle, and a logout button that opens a confirm-logout modal) plus a top bar (section title, service/dinner status, live clock, AI assistant button, profile, and a notifications bell with dropdown). Inside this shell design: the Overview/Dashboard (/) with KPI stat cards (orders today, tables occupied, staff on floor, low inventory) and quick-action buttons; the POS / Point of Sale (/pos) with a menu grid, category pills, search, a "pending table bills" list, and an order/bill sidebar containing a searchable table selector, status badge, cart line items, an Edit Bill mode (quantity steppers, remove, add items), a Parcel/Takeaway toggle, a collapsible price breakdown (subtotal, service charge, tax), a visually dominant Grand Total, primary Charge and secondary Cash buttons, a printer icon that opens a printer-properties popover (Bluetooth/USB connect, kitchen/cashier assignment, test print), and a restaurant-profile modal; the Menu Management screen (/menu) with an item card grid, an add/edit item form (name, price, category, description, image upload, popular/available toggles), a categories manager, and a Customer QR Menu card (QR code with download/copy/open); the Floor Plan / Waiter screen (/tables) showing tables grouped by zone and colored by status (empty, occupied, paying, reserved, attention) with covers and timers; the Table Setup screen (/tables/setup) to add/edit/delete tables (name, zone, capacity); the Kitchen Display System (KDS) (/kds) as a kanban board with New / In-Prep / Ready·Pass columns, order tickets (table, server, items, elapsed timers, VIP and allergy alerts, station tags), a station filter, Fire/Mark-Ready/Delivered actions, and printer/auto-print controls; the Inventory screen (/inventory) with stock items, current-vs-par levels, low-stock indicators, and add/edit; the Staff screen (/staff) with a staff list, active/break/off status, and add-staff; the Reports & Analytics screen (/reports) with a period selector (daily/weekly/monthly/custom range), a KPI strip, a revenue chart/breakdown, sold-items analytics, and a thermal print-report action; a Reservations view (Premium-only, within the manager area) listing and creating reservations (guest, covers, time, table, note, status); the Roles / Role Management screen (/roles) with a user list, add-user form (name, email, role, password, PIN), and a per-screen access matrix (none/view/edit) per user; the Restaurant Profile screen (/profile) for brand name, logo upload, contact, address, tax/VAT settings, currency, language, date format, and receipt footer; and the License / Upgrade screen (/license) showing the current plan, a plan comparison, license-key activation, purchase requests, and trial-days-left.

Dedicated mobile waiter screens: Mobile Orders (/mobile/orders) listing "my tables" and active orders with sign-out; Mobile Table Detail (/mobile/table) showing order line items, totals, a Send-to-Kitchen action, and parcel option; and Mobile Add Item (/mobile/add-item) as a menu picker that adds items to a table's order.

Founder / super-admin console (/founder): a tabbed layout with Overview (platform stat cards), Trial Requests (intake list), License Requests (fulfillment), Restaurants (tenant list), License Keys (generate/list/revoke), and Plan Config (tier pricing and feature toggles).

Shared components and overlays to design as a reusable kit: generic Modal, Upgrade modal, Trial-expired modal, confirm-logout modal, toast notifications (success/error/info), searchable select dropdown, PIN numpad, quantity stepper, status badges (paid/pending/open/sent and table statuses), plan badge, KPI/stat card, QR menu card, menu item image with placeholder, printer help banner, password-visibility toggle, Bluetooth icon, a floating AI support widget, and a full button set (primary, secondary, danger, icon, pill/toggle) plus reusable empty/loading/error state blocks. Use a typography system with a display/serif face for headings, a clean sans for body, and a monospace face for prices and numeric figures, and make the Grand Total, prices, quantities, and order statuses bold and readable from a distance for fast POS use.