/** @deprecated Import from `../i18n` instead — kept for api.ts compatibility. */
export {
  getActiveLanguageCode,
  setActiveLanguageCode,
  applyLanguageToDocument,
  translatePhrase,
  type AppLang,
} from '../i18n';
